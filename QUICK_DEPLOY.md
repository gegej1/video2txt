# ⚡ Video2Txt 快速部署指南

## 🎯 核心问题解答

### ❌ 为什么不能部署到 Vercel？

**简单答案**：Vercel 是为**静态网站**和**短时间 API** 设计的，不适合运行 AI 模型。

**详细原因**：

1. **执行时间限制**
   - Vercel 免费版：10 秒超时
   - Vercel 付费版：60 秒超时
   - 我们的转写：需要几分钟到几十分钟 ❌

2. **无法存储大文件**
   - Whisper 模型：1-3GB
   - Vercel 无法存储 ❌

3. **临时文件系统**
   - 上传的视频会丢失
   - 转写结果无法保存 ❌

4. **计算资源不足**
   - Whisper 需要大量 CPU/内存
   - Vercel Serverless 资源有限 ❌

### ✅ 什么平台可以部署？

**必须满足以下条件**：
- ✅ 支持长时间运行（几分钟到几十分钟）
- ✅ 有持久化存储
- ✅ 可以安装大型依赖（Whisper 模型）
- ✅ 有足够的 CPU 和内存

---

## 🚀 推荐部署方案

### 方案 1: 云服务器（最佳选择 ⭐⭐⭐⭐⭐）

**推荐平台**：
- 🇨🇳 **阿里云**（国内访问快）- ¥60-100/月
- 🇨🇳 **腾讯云**（性价比高）- ¥50-90/月
- 🌍 **Vultr**（国际稳定）- $12-24/月
- 🌍 **DigitalOcean**（简单易用）- $12-24/月

**配置要求**：
```
CPU: 2 核
内存: 4GB
硬盘: 40GB SSD
带宽: 5Mbps+
```

**一键部署脚本**：

```bash
# 1. SSH 连接服务器
ssh root@your_server_ip

# 2. 运行自动部署脚本
curl -fsSL https://raw.githubusercontent.com/你的仓库/deploy.sh | bash
```

---

### 方案 2: Docker 部署（简单快速 ⭐⭐⭐⭐）

**优点**：
- 环境隔离
- 一键启动
- 易于迁移

**部署步骤**：

```bash
# 1. 安装 Docker
curl -fsSL https://get.docker.com | sh

# 2. 进入项目目录
cd video2txt

# 3. 启动服务
docker-compose up -d

# 4. 查看日志
docker-compose logs -f

# 完成！访问 http://服务器IP:5000
```

---

### 方案 3: Railway 一键部署（最简单 ⭐⭐⭐⭐）

**优点**：
- 零配置
- 自动 HTTPS
- GitHub 自动部署
- 免费额度

**部署步骤**：

1. 访问 https://railway.app
2. 登录 GitHub
3. 点击 "New Project"
4. 选择 "Deploy from GitHub repo"
5. 选择你的 video2txt 仓库
6. 等待自动部署（约 5-10 分钟）
7. Railway 会给你一个 URL

**费用**：免费 $5/月，超出按用量付费

---

### 方案 4: 本地 + 内网穿透（临时方案 ⭐⭐）

**适合场景**：
- 临时演示
- 不想花钱
- 自己使用

**使用 ngrok**：

```bash
# 1. 启动本地应用
./start.sh

# 2. 另开终端，安装 ngrok
brew install ngrok  # macOS
# 或访问 https://ngrok.com/download

# 3. 启动隧道
ngrok http 5000

# 你会得到一个公网地址：
# https://xxxx.ngrok.io
```

**注意**：
- ngrok 免费版链接每次重启会变
- 适合临时分享，不适合长期使用

---

## 📊 方案对比

| 方案 | 成本/月 | 难度 | 速度 | 稳定性 | 适合场景 |
|------|---------|------|------|--------|----------|
| **阿里云** | ¥60-100 | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | 长期使用，国内用户 |
| **Docker** | 服务器费用 | ⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | 专业部署 |
| **Railway** | $5-20 | ⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐⭐ | 快速上线 |
| **ngrok** | 免费 | ⭐ | ⭐⭐ | ⭐⭐ | 临时演示 |

---

## 🎯 我应该选择哪个？

### 如果你是...

**学生 / 个人用户**
→ 推荐：**Railway**（简单，有免费额度）

**小团队 / 创业公司**
→ 推荐：**阿里云/腾讯云**（稳定，国内快）

**开发者**
→ 推荐：**Docker + 云服务器**（专业，可控）

**临时演示**
→ 推荐：**ngrok**（零成本，快速）

---

## 🛠️ 详细部署教程

### Railway 部署（推荐新手）

#### 1. 准备 GitHub 仓库

```bash
# 初始化 Git（如果还没有）
git init
git add .
git commit -m "Initial commit"

# 推送到 GitHub
git remote add origin https://github.com/你的用户名/video2txt.git
git push -u origin main
```

#### 2. 部署到 Railway

1. 访问 https://railway.app
2. 点击 "Start a New Project"
3. 选择 "Deploy from GitHub repo"
4. 授权 GitHub 访问
5. 选择 `video2txt` 仓库
6. Railway 自动检测为 Python 项目
7. 等待部署完成（约 10 分钟）

#### 3. 配置环境

Railway 会自动：
- 安装依赖（从 requirements.txt）
- 暴露端口
- 提供 HTTPS 域名

#### 4. 访问应用

部署完成后，点击 "Open App" 或访问 Railway 提供的域名。

---

### 阿里云服务器部署（推荐长期使用）

#### 1. 购买服务器

1. 访问 https://ecs.aliyun.com
2. 选择配置：
   - 地区：选择离你近的
   - 实例规格：2核4GB
   - 镜像：Ubuntu 22.04
   - 公网带宽：5Mbps
3. 设置密码
4. 完成购买

#### 2. 配置安全组

1. 进入 ECS 控制台
2. 点击实例 → 安全组
3. 添加规则：
   - 端口范围：5000
   - 授权对象：0.0.0.0/0
   - 描述：Video2Txt Web

#### 3. 连接服务器

```bash
ssh root@你的服务器IP
# 输入密码
```

#### 4. 一键部署

创建部署脚本 `deploy.sh`：

```bash
#!/bin/bash

echo "开始部署 Video2Txt..."

# 更新系统
apt update && apt upgrade -y

# 安装依赖
apt install -y python3 python3-pip ffmpeg git supervisor nginx

# 克隆代码（或上传）
cd /root
git clone https://github.com/你的用户名/video2txt.git
cd video2txt

# 安装 Python 依赖
pip3 install -r requirements.txt

# 配置 Supervisor
cat > /etc/supervisor/conf.d/video2txt.conf <<EOF
[program:video2txt]
directory=/root/video2txt/app
command=python3 app.py
autostart=true
autorestart=true
stderr_logfile=/var/log/video2txt.err.log
stdout_logfile=/var/log/video2txt.out.log
user=root
EOF

# 启动服务
supervisorctl reread
supervisorctl update
supervisorctl start video2txt

echo "部署完成！"
echo "访问地址: http://$(curl -s ifconfig.me):5000"
```

运行：

```bash
chmod +x deploy.sh
./deploy.sh
```

---

## 🔒 安全建议

1. **配置防火墙**
   ```bash
   ufw allow 5000
   ufw enable
   ```

2. **使用 Nginx 反向代理**
   - 隐藏端口
   - 添加 HTTPS

3. **限制上传大小**
   - 在 app.py 中已设置 500MB
   - 可根据需要调整

4. **定期清理临时文件**
   ```bash
   # 添加到 crontab
   0 2 * * * rm -rf /root/video2txt/uploads/*
   ```

---

## ❓ 常见问题

### Q: 部署后访问很慢？
A:
- 检查服务器位置，选择离用户近的地区
- 使用 CDN 加速静态资源
- 升级服务器带宽

### Q: 模型下载失败？
A:
- 检查服务器网络
- 手动下载模型到 `~/.cache/whisper/`
- 使用国内镜像源

### Q: 内存不足？
A:
- 升级到 4GB 或 8GB 内存
- 使用 Small 模型
- 添加 Swap 交换空间

### Q: 如何绑定域名？
A:
1. 购买域名
2. 添加 A 记录指向服务器 IP
3. 配置 Nginx
4. 使用 Certbot 配置 HTTPS

---

## 📚 相关资源

- [完整部署指南](DEPLOYMENT_GUIDE.md)
- [Docker 使用说明](docker-compose.yml)
- [Nginx 配置示例](DEPLOYMENT_GUIDE.md#step-8-配置-nginx-反向代理)

---

## 💬 获取帮助

遇到问题？

1. 检查服务器日志
2. 查看 [DEPLOYMENT_GUIDE.md](DEPLOYMENT_GUIDE.md)
3. 提交 Issue

---

**开始部署你的 Video2Txt 应用吧！** 🚀
